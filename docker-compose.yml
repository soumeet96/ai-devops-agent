services:
  claw-server:
    build: .
    ports:
      - "3000:3000"
    environment:
      - BIND_ADDR=0.0.0.0:3000
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/sessions"]
      interval: 30s
      timeout: 5s
      retries: 3
