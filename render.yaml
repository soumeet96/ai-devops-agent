services:
  - type: web
    name: ai-devops-agent
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: BIND_ADDR
        value: 0.0.0.0:3000
    healthCheckPath: /sessions
